apiVersion: v1
kind: Service
metadata:
  name: test-web
  namespace: test
spec:
  selector: { app: test-web }
  ports:
    - name: http
      port: 80          # сюда смотрит Ingress
      targetPort: 80
