---
apiVersion: v1
kind: Service
metadata:
  name: demo-app
  labels:
    app.kubernetes.io/name: demo-app
    app.kubernetes.io/part-of: demo-platform
spec:
  selector:
    app.kubernetes.io/name: demo-app
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
