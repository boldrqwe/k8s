apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml          # ← здесь объявлён ТОЛЬКО один ns: test

# (опционально) патчи для теста — см. ниже
# patches:
#   - path: patch-resources.yaml
#   - path: patch-ingress-host.yaml
#   - path: patch-env.yaml
